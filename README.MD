# go-webhook-delivery-service

A webhook delivery service built with Go and RabbitMQ, with a retry mechanism for failed deliveries.

## How to Run

Make sure Docker and Go are installed.

```bash
chmod +x ./run.sh
./run.sh